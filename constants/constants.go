package constants

const (
    AppName = "touchbase"
)

const (
    CommaSeparator = ","

    JsonPrefix = ""
    JsonIntend = "  "
)

// Logging constants.
const (
    JsonFormat    = "json"
    ConsoleFormat = "console"

    LevelKey   = "level"
    TimeKey    = "time"
    MessageKey = "message"
    CallerKey  = "caller"
)
